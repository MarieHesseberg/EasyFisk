# demo keeps the existing prototype clock, scenario controls and simulated payments.
# local uses real time and disables scenario controls and simulated payment issuance.
# Neither mode is a live service. Never put secrets in NEXT_PUBLIC variables.
NEXT_PUBLIC_EASYFISK_MODE=demo
